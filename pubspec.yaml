name: mist
description: Authenticity-first social media app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Third-party packages (Must be aligned with 'flutter:')
  supabase_flutter: ^2.6.0
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.2
  camera: ^0.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Asset section (Uncomment and fix if you add images later)
  # assets:
  #   - assets/images/default_avatar.png